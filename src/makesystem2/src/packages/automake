#!/bin/bash

NAME="automake"
VERSION="1.16.5"

tar -xvf "$NAME-$VERSION.tar.xz"          &&
cd $NAME-$VERSION                         &&
sed -i "s/''/etags/" t/tags-lisp-space.sh &&
./configure --prefix=/usr                 &&
make                                      &&
make install