#!/bin/bash

VERSION="1.22"

tar -xf gdbm-$VERSION.tar.gz
cd gdbm-$VERSION

./configure --prefix=/usr    \
            --disable-static \
            --enable-libgdbm-compat &&
            
make &&
make install
